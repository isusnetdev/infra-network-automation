# Frontend (React) - POC

Este diretório conterá a aplicação React (Vite) que consome a API FastAPI em `../backend`.
Para POC, implemente componentes simples:
- Lista de dispositivos
- Último backup
- Botões para iniciar job (via Semaphore API)
